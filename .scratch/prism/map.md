---
Labels: wayfinder:map
---

# Prism — one design language, many expressions

> **Historical map.** The active clean-break implementation is tracked in
> `.scratch/prism-base-ui/map.md` and `docs/adr/0007-prism-owned-base-ui-runtime.md`.
> The Ant Design foundation, generated pass-throughs, and upstream delegation
> described below are preserved as decision history, not current architecture.

## Destination

prism.nanisoft.com live on Cloudflare Workers — landing page, docs, and blog, all built **with** Prism components and theme — alongside a monorepo whose `@nanisoft/prism-*` packages are publishable to npm (working changesets + CI release pipeline). The first actual npm publish is a task, not the destination.

## Notes

- **Shape**: plasma-shaped pnpm + Turborepo monorepo at github.com/NaniSoft/prism — packages `@nanisoft/prism-tokens`, `@nanisoft/prism-ui`, `@nanisoft/prism-llms`, `@nanisoft/prism-mcp-server`, plus `apps/site`.
- **Foundation**: antd v6 on React 19, TypeScript strict, ESM-only. `PrismProvider` wraps antd `ConfigProvider`; apps always import from `prism-ui`, never antd directly (plasma's rule). `@ant-design/icons` re-exported — no custom icons package.
- **Taxonomy**: components → blocks → pages is an organization taxonomy *inside* `prism-ui` (blocks = pre-composed components; pages = full-page compositions). Everything npm-delivered; code is never copied into consuming apps.
- **Theming**: token architecture is multi-brand from day one (`createPrismTheme()`); v1 ships the NaniSoft brand as **two brand packs — blue and green** (ticket 08), each in light and beam-dark modes.
- **Site**: Fumadocs **headless** (fumadocs-core + fumadocs-mdx `defineDocs`/`defineCollections`; fumadocs-ui styling not used) — Prism-owned `DocsShell`/`BlogLayout` ship as prism-ui pages. Static export → Cloudflare Workers Static Assets. Docs format v1 = rendered demos + copyable source (`ComponentDemo` block).
- **LLM/agent surface**: `prism-llms` (generated `llms.txt` + per-component MD), `prism-mcp-server` (tool-logic package; the `apps/site` Worker serves the HTTP MCP at `/mcp` — one Worker, not two — with an optional stdio shim), ecosystem MCPs in repo `.mcp.json`, agent docs (`AGENTS.md`/`CLAUDE.md`). The antd-skill skills (`ant-design`, `antd`) are already installed in this environment.
- **Figma**: greenfield. One-way token sync code → Figma Variables via a repo-owned plugin fed by DTCG-shaped `prism-tokens` build output (Variables REST API is Enterprise-only; Tokens Studio's git sync is the two-way coupling we forbade). Base library: the free "Ant Design Open Source" file as a **disposable scaffold** restyled via Variables — *no official antd Figma kit exists* (all entries "Third Party"; maintainers won't support Figma). Dev Mode MCP: remote endpoint (`mcp.figma.com`) works on all plans; the desktop endpoint needs a paid seat. **Professional** plan required for libraries + variable modes. No hand-built UI kit; no two-way sync.
- **Toolchain**: pnpm + Turborepo, changesets, oxlint + stylelint, Vitest + RTL, GitHub Actions (PR checks → changeset publish → `wrangler deploy`).
- **Skills to consult**: `ant-design`, `antd`, `cloudflare`, `wrangler`, `workers-best-practices`; `interfaces:*` / `impeccable` for UI direction.
- npm distribution is **public** under the `nanisoft` scope (org-internal in intent, public in availability).

## Decisions so far

<!-- one line per closed ticket — the answer lives in the ticket, this is only the gist + link -->

- [Fumadocs on Workers](issues/03-fumadocs-on-workers.md) — **GO**: fumadocs officially supports static export; DocsShell/BlogLayout are buildable as Prism-owned pages. Macro API is the default `defineDocs` path and forbids re-exporting, so content wiring stays app-level. Fumadocs' MCP endpoint can't live in the static export — docs-MCP tools go on prism-mcp-server, mirroring static `llms.txt` artefacts. Workers config specifics (assets dir, 404 handling, `run_worker_first` for `.md` routes) in the ticket.
- [Plasma conventions](issues/01-plasma-conventions.md) — plasma is a flat `packages/*` monorepo (no `apps/`; its docs site is Storybook, which Prism deliberately replaces with Fumadocs). Packages independently versioned, ESM-only with no bundler. Two mechanics to copy: (1) the "always import from plasma-mantine" invariant is enforced by **codegen** — proxy re-exports generated from upstream Mantine's index — so prism-ui should do the same against antd; (2) changeset validator + per-PR "what would this publish?" preview scripts. `plasma-llms` is a data-only npm package whose hand-maintained per-component MD is the source of truth; one `dist/` serves npm + site + the stdio-only `tmcp` MCP server (6 tools over a bundled `data.json`; antd-coverage delegated to the antd MCP). Prism diverges on MCP transport (Workers) and generates LLM pages from Fumadocs MDX to avoid plasma's duplicated-guidelines drift bug.
- [Figma greenfield setup](issues/04-figma-greenfield-setup.md) — full step-by-step guide in the findings. **No official antd Figma kit exists** — base = the free, stale "Ant Design Open Source" file as a disposable scaffold, restyled via Variables. Token sync = repo-owned Figma plugin fed by DTCG-shaped prism-tokens build output (Variables REST API is Enterprise-only). Dev Mode MCP: remote endpoint on all plans; Professional plan needed for libraries/variable modes; `.mcp.json` entries need `type: "http"` *and* `url` or Claude Code silently skips them.
- [antd v6 × React 19 × Next integration](issues/02-antd-v6-react19-next.md) — the stack works; static export is the *easy* case (`@ant-design/nextjs-registry` bakes antd CSS into the prerendered HTML). Pins: antd ^6.6.4, react ^19.3.0, next ^16.3.5, icons ^6.3.4. Flash-free dark mode: explicit `cssVar.key` per theme (`hashed: false`) + pre-baked variable sets via `@ant-design/static-style-extract` + inline class-swap — `createPrismTheme()` must set `cssVar.key` explicitly and map prism tokens to **seed tokens + algorithms**, never hand-set map tokens. Tailwind conflict moot for headless fumadocs-core. **ProComponents cannot take antd 6 on stable** (only a beta) — dashboard blocks build on plain antd v6; `@ant-design/x ^2.9.0` is healthy and antd-6-only.
- [Remote MCP on Workers](issues/05-remote-mcp-on-workers.md) — one Worker serves the site *and* MCP: `assets.run_worker_first: ["/mcp", "/mcp/*"]` with a stateless `createMcpHandler(factory)` from `agents/mcp/server`. **`McpAgent` is deprecated** (ignore all 2025 tutorials); `workers-mcp` is superseded. Tool logic lives once in a pure `createPrismMcpServer(docs)` factory (HTTP lane + optional `npx mcp-remote` stdio shim), fed by build-time-bundled prism-llms output — no runtime I/O. Auth v1 = none (public read-only docs), with Cloudflare Access pre-written as the org-only escalation. Wrangler gotchas in the ticket.
- [Prism visual language](issues/08-prism-visual-language.md) — **Spectral Refraction**: two brand packs (blue/green), each generating variant-tinted neutrals + beam dark; hairline elevation (one cool shadow, floating layers only); 4px radius; Archivo Variable + JetBrains Mono (width axis as refraction); 80/160/280ms decelerating motion, zero bounce; one accent owns live states; success joins the green pack; dithered-not-blended gradients; named/replayable states. `PRODUCT.md`, `CONTEXT.md`, and ADR-0001 now exist at the repo root. Unblocks ticket 09.
- [Repo bootstrap](issues/06-repo-bootstrap.md) — skeleton live at **github.com/NaniSoft/prism** (private): pnpm 11 + Turborepo 2, four buildable placeholder packages (ESM-only, TS strict on TypeScript 7, publint on publish) + Next static-export site already importing from `prism-ui`. oxlint/stylelint/Vitest+RTL and `pnpm build` green locally and in CI. Changesets wired with a dry-run release pipeline; Version Packages PR open; go-live needs ticket 07. Project-local pnpm store (`.npmrc`) after store-corruption/store-drift incidents. Unblocks ticket 15.
- [Accounts and infra](issues/07-accounts-and-infra.md) — npm org `nanisoft` live; CI credentials stored as **org-level** GitHub secrets (`NPM_TOKEN`, `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID` = `3185310fec380cda3828cbdd9bd26be1`) — invisible to non-admin `gh secret list`, so first release run is the verification. GitHub Actions can now create/approve PRs (toggle: Settings → Actions → General → Workflow permissions). `nanisoft.com` zone already on Cloudflare → Workers Custom Domain auto-creates the `prism.nanisoft.com` record on attach; no manual DNS. Figma Professional workspace "NaniSoft" exists. Credentials deliberately not in a password manager. Unblocks ticket 14; spawns ticket 17 (license).
- [Wire agent tooling](issues/15-wire-agent-tooling.md) — `.mcp.json` wires the real antd MCP (`@ant-design/cli mcp`, stdio, handshake-verified 6.6.0; one-time `/mcp` approval pending). Deliberate deviation, grounded in verified docs: `.mcp.json` is strict JSON with no comment/pending mechanism, so Figma + prism-mcp-server stay OUT as ready-to-paste snippets until tickets 14/13 land them. `AGENTS.md` is now the full agent handbook (taxonomy, conventions, MCP pairing rule, skills); `CLAUDE.md` its pointer-style twin; README gained an Agent workflows section.
- [Token architecture](issues/09-token-architecture-spec.md) — ADR-0002 accepted: `createPrismTheme({pack, mode, overrides?}) → PrismTheme` (antd lane + primitives/semantics as plain data); prism-tokens is pure (no antd types — closed string-literal unions); antd mapping = seeds + algorithms **plus a closed 8-key map-token allowlist** (forced: ADR-0001's 2/4/6/4 radius and 280ms slow are not seed-derivable — refines ticket 02's absolute rule); `components` overrides = shadow-zeroing only; beam-dark = `darkAlgorithm` + tinted `colorBgBase`, never algorithm arrays; Figma = `toDtcg()` over tiers 0–1; cssVar key = `prism-<pack>-<mode>`; brand-ink hexes TBD at the pack pass (AA gate at `defineBrandPack()`). Unblocks ticket 11.
- [Implement ticket 18: ADR-0002 token architecture in prism-tokens](issues/18-implement-tokens-brand-packs.md) — **implementation reviewed against the ADR and corrected (2026-09-19)**: numeric seeds (the first pass set `lineWidth` to a color string and `motionUnit` to `'80ms'`), `hashed: false` at top level, mode-resolved primitives, whole-theme deep-freeze, overrides flowing into the antd lane, text-on-surface AA gate, pack-derived tinted neutrals (blue's values unchanged, green's now green), accent flood as a tint of the ink (not solid ink), DTCG tier 1 as alias *strings* with collision-free Figma names and the shadow composite in `$extensions["prism.shadow"]`, `dist/figma/<pack>/` build output with parity + collision guards, fixture regenerated. Brand hexes stand: ink `#2563EB`/`#3B82F6` and `#0D5C30`/`#22C55E`, green ground `#F0F9F5`, success `#16A34A`. One ADR erratum pending ratification: `focusOutline` left the allowlist (antd v6 makes it a boolean seed) — 16 keys; algorithm lives in prism-ui (prism-tokens stays antd-free).
- [Implement ticket 19: ADR-0003 prism-ui surface](issues/19-implement-prism-ui-surface.md) — **completed (2026-09-19)**; the claimed first pass didn't compile (extensionless imports, missing prism-tokens dep, no-arg `createPrismTheme()` call). Landed: `PrismProvider` with `prismTheme?: PrismTheme` selection + consumer-last per-key merge (`mergePrismTheme` attaches the mode's antd algorithm), full registry catalog (antdBase + antd's six categories + Prism group; `buildCatalog()` throws on gaps) feeding site stubs + prism-llms, parity tests pinned to installed antd in both directions, the ADR-0002 `getDesignToken()` tripwire, blocks/pages as real directories making the wildcard exports resolve, DisplayTitle per the ADR worked example, `@layer prism { … }` actually wrapping the rules, `./prototype` stand-in subpath. All 13 turbo tasks green including the site.
- [prism-ui API conventions](issues/10-prism-ui-api-conventions.md) — ADR-0003 accepted: subpath layers `/components|blocks|pages` + per-item entries; **pass-through by default via codegen from installed antd** (hermetic; bump PRs diff the delta); wrappers gated by a closed 4-item justification list + registry; antd is a direct dependency (pnpm isolation makes the invariant physical); `PrismProvider` = ConfigProvider → App → children, consumer-last theme merge, locale/direction forward-only; pages take Prism-owned structural types (no next/fumadocs deps — apps ship `toPrismTree()`); CSS app-imported in `@layer prism`; no `@ant-design/x` in v1; RTL forward-only, documented untested. Unblocks ticket 12.
- [MCP tool surface](issues/13-mcp-tool-surface.md) — ADR-0004 accepted: eight read-only tools (`list_items`, `get_item_doc`, `get_item_props`, `get_item_source`, `get_theme_doc`, `list_pages`, `get_page`, `search_docs`), unprefixed names; antd's upstream surface delegates to the antd MCP (the per-item `Extends` line is the routing rule); `PrismDocsStore` fixed here as the contract ticket 16 projects into; auth none in v1 with the Cloudflare Access escalation pre-written. Feeds ticket 16 (still gated on 12).
- [Public packages license](issues/17-public-packages-license.md) — **MIT, uniform across all four packages**; copyright "NaniSoft"; per-package + root `LICENSE`, third-party attribution in `THIRD-PARTY-NOTICES.md`; brand protection = separate policy page, never license clauses; no trademark filing now; DCO when the first external PR arrives. Premise corrected: npm accepts `UNLICENSED` publishes — the license lands for legal coherence (ticket 07 amended). Mechanical LICENSE/`package.json` edits land in the go-live pass, before the `release-pr.yml` swap.
- [Landing page design](issues/11-landing-page-design.md) — **Specimen wins**: dark-first, antd worn loudly — the hero *is* a live themed specimen plate; taxonomy as spatial progression bands (01 components → 02 blocks → 03 pages); light mode appears once as the "docs in daylight" peek, not a toggle. Ledger (light editorial) and Workbench (live pack × mode) declined; the full set is preserved on branch `prototype/landing-variants` — raid it for the real page. Variant A stands in on the site's `/`; the real prism-ui page lands in the site-build pass.
- [Docs IA and demo format](issues/12-docs-ia-and-demo-format.md) — flat taxonomy URLs (`/docs`, `/components`, `/blocks`, `/pages`, `/blog`; four doc loaders + blog); thin generated stub pages for every pass-through (codegen emits stub MDX + `meta.json` from the category table; uncovered component fails CI), full template for wrapped/blocks/pages; sidebars = Prism group + antd's six categories; page skeleton = import line → Extends → when-to-use (only prose) → ComponentDemo sequence → generated API tables → prev/next + TOC; ComponentDemo = live preview + copy/expand bar, each example one self-contained `.tsx` (`demos/*.tsx`, slug = example id — the file three consumers read); API tables generated from prism-ui types + TSDoc; shell-level pack × mode switcher defaulting beam-dark blue; frontmatter = fumadocs defaults only (kind/antdBase derived, never authored); prism-llms is the one LLM surface served at the root (fumadocs `llms()`/`/api/mcp` un-generated; `.md` rewrites hit prism-llms artifacts); blog = folder-per-post with date/tags/draft, display-only tags, summary RSS; PrismLocale slot lands with the first shipped Prism string (DocsShell chrome or the copy button — both namespaces specced). Unblocks ticket 16; site-build fog graduates once 16 closes.
- [prism-llms shape](issues/16-prism-llms-shape.md) — one build, one `dist/`, three lanes: `data.json` (the PrismDocsStore projection, MCP lane), `llms.txt` + `llms-full.txt` (agent reference corpus = guides + catalog + themes; **blog excluded**), and an `md/` mirror tree serving all five sections per-page (blog lane-only). Authority: MDX prose · prism-ui `dist` `.d.ts` props read by the **extractor homed in prism-llms** (exported for the site's API tables) · `demos/*.tsx` verbatim · `antdBase` from `wrapped-registry` · **4 theme atoms** from prism-tokens. Drift gate = **build-fresh, dist never committed**, plus `prism-llms#check` with 7 invariants (coverage, demo contract, cross-refs, descriptions, store validation, link/mirror completeness, build-twice byte-compare). Worker `.md` rewrite = one prefix rule (`/<section>/<slug>.md` → `/md/<section>/<slug>.md`). No `npx` CLI in v1. PrismDocsStore's type canonically lives in prism-mcp-server; prism-llms devDeps it type-only for validation. **Graduated the whole build route as tickets 18–23.**
- [Implement ticket 20: prism-llms generator + drift gate](issues/20-implement-prism-llms-generator.md) — **implemented (2026-09-19), 15/15 turbo tasks green.** `emit(outDir)` produces the three lanes byte-stably; the props extractor (`./extractor`) is **compiler-API-free** (TS 7 dropped the JS API — a structural `.d.ts` scanner instead, ADR-0003's cosmetic-risk clause); pass-throughs carry no `props` field, their MDX ends on the `_No additional props…_` seam line; cross-refs derived from demo imports; theme atoms render the snippet + tier tables + antd token lane. **`PrismDocsStore` + `parsePrismDocsStore` landed in prism-mcp-server** (JSON imports widen `kind` to `string`, so the runtime guard does the literal validation; ticket 22's factory reuses it). prism-ui codegen emits `pass-throughs.ts`; `./wrapped-registry`/`./generated/pass-throughs` subpaths keep one `buildCatalog()`. `prism-llms#check` runs the 7 invariants via a turbo `check` task + CI step (`pnpm check` locally). The **stub generator** (`generate-content`) committed 75 stubs + section `meta.json`s and only ever rewrites marker-carrying files — ticket 21 wires its `generate` task to it. Content contract fixed: `content/{docs,components,blocks,pages,blog}/…`, `description` frontmatter required. Demo pipeline smoke-tested end-to-end. Unblocks ticket 21.
- [Site build & deploy](issues/21-site-build-deploy.md) — **prism.nanisoft.com is live** (2026-09-19; Worker `prism-site`, Custom Domain attached, DNS auto-created as predicted). Five loaders with optional-catch-all routes (the trick that lets empty docs/blog sections build under static export); full doc template per ticket 12; Specimen landing rebuilt as a real prism-ui page; flash-free theming per ticket 02 (four pre-baked ~250KB variable rulesets, blocking class-swap boot script, beam-dark blue default); Worker = the one `.md` prefix rewrite + **glob** `run_worker_first` (route-style `:slug*.md` patterns silently never match — live-learned gotcha) + a 501 `/mcp` seam ticket 22 replaced. prism-ui's dangling `./styles.css` export fixed at the source (patch changeset). Deferred: site search UI (12 §4), PrismLocale prism-ui home, `prototype` module removal → ticket 23.
- [Implement prism-mcp-server](issues/22-implement-prism-mcp-server.md) — **the eight-tool MCP is live at `/mcp`** (2026-09-20, verified over real protocol round-trips). Pure `createPrismMcpServer(docs, {built?})` over the bundled `data.json` (leaf package intact); build date stamped from the corpus's own build mtime via a committed generator — controller ruling kept prism-llms untouched (a store `built` field remains the clean long-term home); runtime `ExecutionContext` threads entry → router → handler; transport exactly as ADR §6 (`allowedHostnames`, no CORS, json); real-protocol tests on both lanes (`InMemoryTransport` + Worker). stdio = `npx mcp-remote` doc-only; auth none, CF Access pre-written. The `.mcp.json` prism entry is now eligible — lands in ticket 23's sweep.
- [Implement ticket 21: site build & deploy](issues/21-site-build-deploy.md) — **implemented and deployed (2026-09-19): prism.nanisoft.com live** (one Worker `prism-site` + Custom Domain, DNS auto-created). Site: five per-section fumadocs loaders behind optional catch-all routes (the optional root is what lets empty sections build under `output: export`); section catalogs grouped from prism-ui's `buildCatalog()` table; DocsShell/BlogLayout wired; Specimen landing rebuilt as a real prism-ui page; flash-free pack × mode switcher (four pre-baked `prism-<pack>-<mode>` variable rulesets + blocking class-swap script, default beam-dark blue); app-level `Demo` wire over a generated demos registry; RSS at `/rss.xml`; `llms.txt` + `md/**` copied into `out/`. Worker: the one `.md` prefix rule (`run_worker_first` needs **glob** patterns — route-style `:slug*.md` silently never matches) and a 501 `/mcp` seam module that ticket 22 swaps. prism-ui's dangling `./styles.css` export now really ships (fonts live in `apps/site/public/fonts`). Known noise: Next 16 prefetch payloads for catch-all roots 404 (navigation falls back cleanly). Unblocks ticket 22.
- [Go-live](issues/23-go-live.md) — **destination reached (2026-09-20)**: all four `@nanisoft/prism-*` packages published to npm at 0.2.0 (registry-verified, MIT, release tags pushed); MIT mechanicals + `THIRD-PARTY-NOTICES.md` + OFL texts beside the fonts; `/docs/brand` policy page + footer line; prism entry wired into `.mcp.json` with `AGENTS.md`/`CLAUDE.md` agreeing; hygiene sweep landed; site redeployed with all of it live. Gotchas in the ticket: the two workflow files carry swapped names (`release-pr.yml` = Changesets version-PR lane, `cd.yml` = the former dry-run, now the real publish lane); npm publish needs a granular token with 2FA bypass from an org-owner account (CI 404 = token's account not in the org). **One open follow-up**: the org secret `NPM_TOKEN` still isn't injected into the private repo (scoped "Public repositories") — fix org-secret visibility before trusting the CI publish lane; until then release manually via `pnpm build && pnpm release`.
- [Pastel pack expansion](issues/24-pastel-pack-expansion.md) — **shipped (2026-09-20)**: the two-pack offering became the **pastel five-pack spectrum** (blue + green reforged in pastel voice, ids kept; lavender, rose, peach new) per the user's locked "reforge + 3 new" direction — pastel is the atmosphere (grounds/hairlines/washes/beam tints), inks stay AA-gate mid-tones. ADR-0005 accepted (with the 2nd ADR-0002 erratum: 17th allowlist key `colorTextPlaceholder` → textTertiary); PRODUCT.md brand commitment updated. Site: ten pre-baked rulesets, swatch-dot switcher, `/themes` gallery of live themed islands with a "Wear this theme" hand-off, landing spectrum band, pack-following daylight peek. Bake/figma/llms scripts derive pack loops from `prismBrandPacks` — the next pack is one data entry. Changeset: minor ×4. Graduate-worthy follow-ups: landing mobile capture is proven only on the gallery route; the theme gallery could later host third-party packs (see Not-yet-specified).
- [Site first-use and truthfulness hardening](issues/25-site-first-use-and-truthfulness.md) — **resolved (2026-09-25)**: the landing search/specimen controls are live, the human and agent doors are explicit (`/docs/quickstart`, `/llms.txt`, and the `POST /mcp` setup), theme islands compare light/beam-dark pairs with local controls and announced shell hand-offs, and the docs generator wires demos when present while giving unregistered items an explicit MCP recovery state. Token/opacity/shadow/mask drift and clipboard/theme accessibility gaps were closed; `turbo.json` orders the llms drift check after the site generator to keep the determinism gate stable. Browser visual inspection remains unavailable without a connected desktop browser.

## Not yet specified

<!-- Decided 2026-09-15: with 16 closed, the entire build route is specifiable — graduated as task tickets 18–23 (tokens → prism-ui → {llms generator, site} → mcp-server → go-live; blocking edges on the tickets). Ticket 14 stays claimed on the human Figma checklist and blocks nothing on the build route. **Go-live shipped 2026-09-20 — the destination is reached.** The fog below is post-launch material: it graduates into a fresh effort's tickets if and when wanted; nothing here is owed by this map. -->

- **Dashboard/admin catalog** — shells and CRUD blocks for dashboards/admin sites. Note from ticket 02: ProComponents' stable line **cannot** take antd v6 (only a beta), so these blocks build on plain antd v6 (or wait for stable ProComponents) — that choice is part of what this fog must resolve when it graduates after the site ships.
- **Live-edit playground** — editable demo pane (react-live-style). Rendered demos + source copy ship in v1; the playground graduates only if wanted post-launch.
- **Visual-regression testing** for `prism-ui` — tool choice hangs on prism-ui carrying real component UI worth snapshotting (post tickets 10–11), not on CI plumbing; CI is proven green by the bootstrap.
- **Theme gallery + external brand pack** — the in-house half graduated in ticket 24: `/themes` shows all five packs × two modes live. What still graduates later is the *third-party* brand pack proving `createPrismTheme()` for strangers (and its home in the gallery); hangs on nothing.
- **Blog content plan** — voice, categories, first posts; hangs on `BlogLayout` existing.
- **Site i18n / locale strategy** — antd has locale machinery, but nothing demands it yet.
- **Corpus-delta PR comments** — a CI job posting "this PR changes 4 component docs, adds 1 block" summaries of the prism-llms corpus delta without committing artifacts (declined from ticket 16 in favor of the lean invariant gate; review visibility can graduate from here if wanted).

## Out of scope

- **Multi-platform outputs (React Native etc.)** — a separate future effort, not this map.
- **Custom icons package** — deliberately skipped; `@ant-design/icons` is re-exported through prism-ui.
- **Two-way Figma ↔ code token sync** — tar pit; code → Figma only.
- **Hand-built Prism Figma UI kit** — the free "Ant Design Open Source" file, restyled via Variables, instead (research found no official antd kit exists to restyle).
- **SSR / OpenNext for the site** — static export chosen; revisit only if the site gains real server-side behavior.
