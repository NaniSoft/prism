{
  "name": "@nanisoft/prism-tokens",
  "version": "0.1.0",
  "description": "Design tokens for Prism — the single source of truth for the Prism design language.",
  "license": "UNLICENSED",
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=22"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "oxlint .",
    "test": "vitest run",
    "prepublishOnly": "publint"
  },
  "devDependencies": {
    "publint": "^0.3.24",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
