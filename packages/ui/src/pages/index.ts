/**
 * Every Prism Page.
 *
 * A Page is a shipped, installable screen model composed of Blocks and
 * Components that receives application-owned navigation, content and data
 * (ticket 07). The v1 roster ships four Pages; this entry starts empty and
 * grows one subpath at a time as `pages/<slug>/` directories land.
 */
export {}
