---
"@nanisoft/prism-llms": patch
---

Make the site’s docs corpus honest about demo coverage: generated item pages now surface a live demo when one exists and an explicit MCP recovery path when one does not. The site adds a quickstart and representative Button, Input, and Switch examples so the first-use path is demonstrable.
