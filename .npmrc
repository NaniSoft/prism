shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true

# pnpm 11 keeps registry and auth settings in .npmrc and every other setting in
# pnpm-workspace.yaml, so the project-local store is configured there as
# `storeDir`. The store directory is gitignored and cached in CI.
