---
title: Radii
description: "One base radius drives the whole scale, derived as calc() multipliers."
---

A single `--radius` token is the source, and the scale is derived from it. Each
pack overrides the base, so changing a pack changes every rounded corner
together.

<TokenTable group="radius" />
