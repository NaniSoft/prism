---
title: Motion
description: "State feedback only, by token, with no entrance or scroll animation."
---

Motion is state feedback. It is never decorative, there are no keyframes, and a
component names a duration token and an easing token rather than a value.
Spatial movement is guarded with `motion-safe:` so a reduced-motion preference
removes it.

<TokenTable group="motion" />
