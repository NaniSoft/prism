---
title: Variables
description: "The full token browser: every semantic custom property and every foundation group."
---

This is the token inventory. It reads the emitted JSON rather than a hardcoded
table, so it is the same data the stylesheet is compiled from. Colours are shown
as light and dark pairs; the mode-independent groups are shown once.

<TokenBrowser />
