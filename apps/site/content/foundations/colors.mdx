---
title: Colors
description: "The semantic colour contract and the foundation ramps behind it."
---

Prism ships two colour families: a set of semantic roles whose names are the
shadcn variable contract, and the foundation ramps those roles alias into. A
component consumes a semantic utility, never a ramp step, which is what makes
theming automatic at runtime.

<ColorTokens />
