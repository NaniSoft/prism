---
title: "SettingsPanel"
description: "A sectioned settings form with sticky save state."
---
{/* prism:generated-owned v2 */}

```ts
import { SettingsPanel } from '@nanisoft/prism-ui/blocks/settings-panel';
```

SettingsPanel is a Prism-owned block. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.

<ComponentDemo id="basic" title="SettingsPanel" />
