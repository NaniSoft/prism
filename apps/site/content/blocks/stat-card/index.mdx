---
title: "StatCard"
description: "A labelled business signal with context and directional change."
---
{/* prism:generated-owned v2 */}

```ts
import { StatCard } from '@nanisoft/prism-ui/blocks/stat-card';
```

StatCard is a Prism-owned block. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.
