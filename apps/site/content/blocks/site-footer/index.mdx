---
title: SiteFooter
description: Prism block — full documentation pending.
---
{/* prism:generated-stub v1 */}

```ts
import { SiteFooter } from '@nanisoft/prism-ui/blocks';
```

`SiteFooter` is a Prism block. Full documentation is pending — the prism MCP answers API questions in the meantime (`get_item_props`, `get_item_source`).
