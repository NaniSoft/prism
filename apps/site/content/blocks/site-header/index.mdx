---
title: "SiteHeader"
description: "A responsive product header with theme controls and mobile navigation."
---
{/* prism:generated-owned v2 */}

```ts
import { SiteHeader } from '@nanisoft/prism-ui/blocks/site-header';
```

SiteHeader is a Prism-owned block. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.

<ComponentDemo id="basic" title="SiteHeader" />
