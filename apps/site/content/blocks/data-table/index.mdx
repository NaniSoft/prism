---
title: "DataTable"
description: "A typed table shell with toolbar, bulk actions, and pagination."
---
{/* prism:generated-owned v2 */}

```ts
import { DataTable } from '@nanisoft/prism-ui/blocks/data-table';
```

DataTable is a Prism-owned block. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.

<ComponentDemo id="basic" title="DataTable" />
