---
title: "ApplicationShell"
description: "Sidebar, top bar, and content frame for an application surface."
---
{/* prism:generated-owned v2 */}

```ts
import { ApplicationShell } from '@nanisoft/prism-ui/blocks/application-shell';
```

ApplicationShell is a Prism-owned block. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.

<ComponentDemo id="basic" title="ApplicationShell" />
