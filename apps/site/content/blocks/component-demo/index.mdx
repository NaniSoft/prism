---
title: "ComponentDemo"
description: "A live example paired with its verbatim source."
---
{/* prism:generated-owned v2 */}

```ts
import { ComponentDemo } from '@nanisoft/prism-ui/blocks/component-demo';
```

ComponentDemo is a Prism-owned block. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.
