---
title: "PageHeader"
description: "A page title, context, breadcrumb, and action region."
---
{/* prism:generated-owned v2 */}

```ts
import { PageHeader } from '@nanisoft/prism-ui/blocks/page-header';
```

PageHeader is a Prism-owned block. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.
