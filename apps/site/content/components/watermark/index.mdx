---
title: Watermark
description: antd Watermark, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Watermark } from '@nanisoft/prism-ui/components';
```

`Watermark` is antd's [Watermark](https://ant.design/components/watermark), re-exported unchanged.

- Extends: antd **Watermark** — for inherited props and demos, use the antd MCP (`antd_info Watermark`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
