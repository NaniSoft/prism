---
title: "RadioGroup"
description: "A labelled group for one choice from a small visible set."
---
{/* prism:generated-owned v2 */}

```ts
import { RadioGroup } from '@nanisoft/prism-ui/components/radio-group';
```

RadioGroup is a Prism-owned component. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.
