---
title: Rate
description: antd Rate, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Rate } from '@nanisoft/prism-ui/components';
```

`Rate` is antd's [Rate](https://ant.design/components/rate), re-exported unchanged.

- Extends: antd **Rate** — for inherited props and demos, use the antd MCP (`antd_info Rate`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
