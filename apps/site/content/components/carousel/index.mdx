---
title: Carousel
description: antd Carousel, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Carousel } from '@nanisoft/prism-ui/components';
```

`Carousel` is antd's [Carousel](https://ant.design/components/carousel), re-exported unchanged.

- Extends: antd **Carousel** — for inherited props and demos, use the antd MCP (`antd_info Carousel`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
