---
title: Avatar
description: antd Avatar, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Avatar } from '@nanisoft/prism-ui/components';
```

`Avatar` is antd's [Avatar](https://ant.design/components/avatar), re-exported unchanged.

- Extends: antd **Avatar** — for inherited props and demos, use the antd MCP (`antd_info Avatar`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
