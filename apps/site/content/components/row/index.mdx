---
title: Row
description: antd Row, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Row } from '@nanisoft/prism-ui/components';
```

`Row` is antd's [Row](https://ant.design/components/row), re-exported unchanged.

- Extends: antd **Row** — for inherited props and demos, use the antd MCP (`antd_info Row`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
