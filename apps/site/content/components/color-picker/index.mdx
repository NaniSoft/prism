---
title: ColorPicker
description: antd ColorPicker, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { ColorPicker } from '@nanisoft/prism-ui/components';
```

`ColorPicker` is antd's [ColorPicker](https://ant.design/components/color-picker), re-exported unchanged.

- Extends: antd **ColorPicker** — for inherited props and demos, use the antd MCP (`antd_info ColorPicker`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
