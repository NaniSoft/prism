---
title: Drawer
description: antd Drawer, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Drawer } from '@nanisoft/prism-ui/components';
```

`Drawer` is antd's [Drawer](https://ant.design/components/drawer), re-exported unchanged.

- Extends: antd **Drawer** — for inherited props and demos, use the antd MCP (`antd_info Drawer`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
