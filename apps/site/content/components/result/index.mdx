---
title: Result
description: antd Result, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Result } from '@nanisoft/prism-ui/components';
```

`Result` is antd's [Result](https://ant.design/components/result), re-exported unchanged.

- Extends: antd **Result** — for inherited props and demos, use the antd MCP (`antd_info Result`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
