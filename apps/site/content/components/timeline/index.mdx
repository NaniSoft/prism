---
title: Timeline
description: antd Timeline, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Timeline } from '@nanisoft/prism-ui/components';
```

`Timeline` is antd's [Timeline](https://ant.design/components/timeline), re-exported unchanged.

- Extends: antd **Timeline** — for inherited props and demos, use the antd MCP (`antd_info Timeline`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
