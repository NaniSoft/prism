---
title: Affix
description: antd Affix, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Affix } from '@nanisoft/prism-ui/components';
```

`Affix` is antd's [Affix](https://ant.design/components/affix), re-exported unchanged.

- Extends: antd **Affix** — for inherited props and demos, use the antd MCP (`antd_info Affix`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
