---
title: Progress
description: antd Progress, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Progress } from '@nanisoft/prism-ui/components';
```

`Progress` is antd's [Progress](https://ant.design/components/progress), re-exported unchanged.

- Extends: antd **Progress** — for inherited props and demos, use the antd MCP (`antd_info Progress`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
