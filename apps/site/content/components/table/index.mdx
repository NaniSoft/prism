---
title: Table
description: antd Table, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Table } from '@nanisoft/prism-ui/components';
```

`Table` is antd's [Table](https://ant.design/components/table), re-exported unchanged.

- Extends: antd **Table** — for inherited props and demos, use the antd MCP (`antd_info Table`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
