---
title: Calendar
description: antd Calendar, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Calendar } from '@nanisoft/prism-ui/components';
```

`Calendar` is antd's [Calendar](https://ant.design/components/calendar), re-exported unchanged.

- Extends: antd **Calendar** — for inherited props and demos, use the antd MCP (`antd_info Calendar`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
