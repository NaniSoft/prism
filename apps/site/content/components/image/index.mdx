---
title: Image
description: antd Image, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Image } from '@nanisoft/prism-ui/components';
```

`Image` is antd's [Image](https://ant.design/components/image), re-exported unchanged.

- Extends: antd **Image** — for inherited props and demos, use the antd MCP (`antd_info Image`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
