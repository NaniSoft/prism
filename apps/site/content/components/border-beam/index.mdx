---
title: BorderBeam
description: antd BorderBeam, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { BorderBeam } from '@nanisoft/prism-ui/components';
```

`BorderBeam` is antd's [BorderBeam](https://ant.design/components/border-beam), re-exported unchanged.

- Extends: antd **BorderBeam** — for inherited props and demos, use the antd MCP (`antd_info BorderBeam`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
