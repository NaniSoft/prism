---
title: BackTop
description: antd BackTop, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { BackTop } from '@nanisoft/prism-ui/components';
```

`BackTop` is antd's [BackTop](https://ant.design/components/back-top), re-exported unchanged.

- Extends: antd **BackTop** — for inherited props and demos, use the antd MCP (`antd_info BackTop`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
