---
title: Form
description: antd Form, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Form } from '@nanisoft/prism-ui/components';
```

`Form` is antd's [Form](https://ant.design/components/form), re-exported unchanged.

- Extends: antd **Form** — for inherited props and demos, use the antd MCP (`antd_info Form`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
