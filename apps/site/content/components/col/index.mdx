---
title: Col
description: antd Col, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Col } from '@nanisoft/prism-ui/components';
```

`Col` is antd's [Col](https://ant.design/components/col), re-exported unchanged.

- Extends: antd **Col** — for inherited props and demos, use the antd MCP (`antd_info Col`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
