---
title: Divider
description: antd Divider, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Divider } from '@nanisoft/prism-ui/components';
```

`Divider` is antd's [Divider](https://ant.design/components/divider), re-exported unchanged.

- Extends: antd **Divider** — for inherited props and demos, use the antd MCP (`antd_info Divider`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
