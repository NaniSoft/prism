---
title: Flex
description: antd Flex, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Flex } from '@nanisoft/prism-ui/components';
```

`Flex` is antd's [Flex](https://ant.design/components/flex), re-exported unchanged.

- Extends: antd **Flex** — for inherited props and demos, use the antd MCP (`antd_info Flex`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
