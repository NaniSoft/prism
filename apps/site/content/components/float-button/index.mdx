---
title: FloatButton
description: antd FloatButton, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { FloatButton } from '@nanisoft/prism-ui/components';
```

`FloatButton` is antd's [FloatButton](https://ant.design/components/float-button), re-exported unchanged.

- Extends: antd **FloatButton** — for inherited props and demos, use the antd MCP (`antd_info FloatButton`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
