---
title: Modal
description: antd Modal, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Modal } from '@nanisoft/prism-ui/components';
```

`Modal` is antd's [Modal](https://ant.design/components/modal), re-exported unchanged.

- Extends: antd **Modal** — for inherited props and demos, use the antd MCP (`antd_info Modal`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
