---
title: Tag
description: antd Tag, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Tag } from '@nanisoft/prism-ui/components';
```

`Tag` is antd's [Tag](https://ant.design/components/tag), re-exported unchanged.

- Extends: antd **Tag** — for inherited props and demos, use the antd MCP (`antd_info Tag`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
