---
title: Steps
description: antd Steps, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Steps } from '@nanisoft/prism-ui/components';
```

`Steps` is antd's [Steps](https://ant.design/components/steps), re-exported unchanged.

- Extends: antd **Steps** — for inherited props and demos, use the antd MCP (`antd_info Steps`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
