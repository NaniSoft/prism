---
title: TreeSelect
description: antd TreeSelect, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { TreeSelect } from '@nanisoft/prism-ui/components';
```

`TreeSelect` is antd's [TreeSelect](https://ant.design/components/tree-select), re-exported unchanged.

- Extends: antd **TreeSelect** — for inherited props and demos, use the antd MCP (`antd_info TreeSelect`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
