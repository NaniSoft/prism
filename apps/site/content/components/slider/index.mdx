---
title: "Slider"
description: "A keyboard-operable numeric slider with a visible value option."
---
{/* prism:generated-owned v2 */}

```ts
import { Slider } from '@nanisoft/prism-ui/components/slider';
```

Slider is a Prism-owned component. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.
