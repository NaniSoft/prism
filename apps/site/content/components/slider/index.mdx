---
title: Slider
description: antd Slider, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Slider } from '@nanisoft/prism-ui/components';
```

`Slider` is antd's [Slider](https://ant.design/components/slider), re-exported unchanged.

- Extends: antd **Slider** — for inherited props and demos, use the antd MCP (`antd_info Slider`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
