---
title: QRCode
description: antd QRCode, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { QRCode } from '@nanisoft/prism-ui/components';
```

`QRCode` is antd's [QRCode](https://ant.design/components/qrcode), re-exported unchanged.

- Extends: antd **QRCode** — for inherited props and demos, use the antd MCP (`antd_info QRCode`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
