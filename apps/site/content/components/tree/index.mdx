---
title: Tree
description: antd Tree, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Tree } from '@nanisoft/prism-ui/components';
```

`Tree` is antd's [Tree](https://ant.design/components/tree), re-exported unchanged.

- Extends: antd **Tree** — for inherited props and demos, use the antd MCP (`antd_info Tree`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
