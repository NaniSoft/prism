---
title: Layout
description: antd Layout, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Layout } from '@nanisoft/prism-ui/components';
```

`Layout` is antd's [Layout](https://ant.design/components/layout), re-exported unchanged.

- Extends: antd **Layout** — for inherited props and demos, use the antd MCP (`antd_info Layout`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
