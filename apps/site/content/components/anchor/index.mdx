---
title: Anchor
description: antd Anchor, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Anchor } from '@nanisoft/prism-ui/components';
```

`Anchor` is antd's [Anchor](https://ant.design/components/anchor), re-exported unchanged.

- Extends: antd **Anchor** — for inherited props and demos, use the antd MCP (`antd_info Anchor`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
