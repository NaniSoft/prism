---
title: Input
description: antd Input, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Input } from '@nanisoft/prism-ui/components';
```

`Input` is antd's [Input](https://ant.design/components/input), re-exported unchanged.

- Extends: antd **Input** — for inherited props and demos, use the antd MCP (`antd_info Input`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
