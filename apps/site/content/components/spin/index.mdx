---
title: Spin
description: antd Spin, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Spin } from '@nanisoft/prism-ui/components';
```

`Spin` is antd's [Spin](https://ant.design/components/spin), re-exported unchanged.

- Extends: antd **Spin** — for inherited props and demos, use the antd MCP (`antd_info Spin`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
