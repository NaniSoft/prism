---
title: Listy
description: antd Listy, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Listy } from '@nanisoft/prism-ui/components';
```

`Listy` is antd's [Listy](https://ant.design/components/listy), re-exported unchanged.

- Extends: antd **Listy** — for inherited props and demos, use the antd MCP (`antd_info Listy`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
