---
title: Cascader
description: antd Cascader, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Cascader } from '@nanisoft/prism-ui/components';
```

`Cascader` is antd's [Cascader](https://ant.design/components/cascader), re-exported unchanged.

- Extends: antd **Cascader** — for inherited props and demos, use the antd MCP (`antd_info Cascader`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
