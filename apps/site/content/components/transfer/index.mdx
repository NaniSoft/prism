---
title: Transfer
description: antd Transfer, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Transfer } from '@nanisoft/prism-ui/components';
```

`Transfer` is antd's [Transfer](https://ant.design/components/transfer), re-exported unchanged.

- Extends: antd **Transfer** — for inherited props and demos, use the antd MCP (`antd_info Transfer`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
