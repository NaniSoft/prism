---
title: AutoComplete
description: antd AutoComplete, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { AutoComplete } from '@nanisoft/prism-ui/components';
```

`AutoComplete` is antd's [AutoComplete](https://ant.design/components/auto-complete), re-exported unchanged.

- Extends: antd **AutoComplete** — for inherited props and demos, use the antd MCP (`antd_info AutoComplete`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
