---
title: Typography
description: antd Typography, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Typography } from '@nanisoft/prism-ui/components';
```

`Typography` is antd's [Typography](https://ant.design/components/typography), re-exported unchanged.

- Extends: antd **Typography** — for inherited props and demos, use the antd MCP (`antd_info Typography`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
