---
title: TimePicker
description: antd TimePicker, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { TimePicker } from '@nanisoft/prism-ui/components';
```

`TimePicker` is antd's [TimePicker](https://ant.design/components/time-picker), re-exported unchanged.

- Extends: antd **TimePicker** — for inherited props and demos, use the antd MCP (`antd_info TimePicker`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
