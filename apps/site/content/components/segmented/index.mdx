---
title: Segmented
description: antd Segmented, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Segmented } from '@nanisoft/prism-ui/components';
```

`Segmented` is antd's [Segmented](https://ant.design/components/segmented), re-exported unchanged.

- Extends: antd **Segmented** — for inherited props and demos, use the antd MCP (`antd_info Segmented`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
