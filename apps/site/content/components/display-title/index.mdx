---
title: DisplayTitle
description: Prism wrapper around antd Typography.Title (brand-behavior).
---
{/* prism:generated-stub v1 */}

```ts
import { DisplayTitle } from '@nanisoft/prism-ui/components';
```

`DisplayTitle` is a Prism wrapper around antd **Typography.Title**.

- Extends: antd **Typography.Title** — for inherited props, use the antd MCP (`antd_info Typography.Title`).
- Prism-added props are listed by the prism MCP (`get_item_props { "name": "DisplayTitle" }`).

_Full documentation pending — this stub is honest, not broken._
