---
title: Upload
description: antd Upload, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Upload } from '@nanisoft/prism-ui/components';
```

`Upload` is antd's [Upload](https://ant.design/components/upload), re-exported unchanged.

- Extends: antd **Upload** — for inherited props and demos, use the antd MCP (`antd_info Upload`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
