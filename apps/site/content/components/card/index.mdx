---
title: Card
description: antd Card, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Card } from '@nanisoft/prism-ui/components';
```

`Card` is antd's [Card](https://ant.design/components/card), re-exported unchanged.

- Extends: antd **Card** — for inherited props and demos, use the antd MCP (`antd_info Card`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
