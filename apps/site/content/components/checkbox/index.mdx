---
title: Checkbox
description: antd Checkbox, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Checkbox } from '@nanisoft/prism-ui/components';
```

`Checkbox` is antd's [Checkbox](https://ant.design/components/checkbox), re-exported unchanged.

- Extends: antd **Checkbox** — for inherited props and demos, use the antd MCP (`antd_info Checkbox`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
