---
title: Menu
description: antd Menu, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Menu } from '@nanisoft/prism-ui/components';
```

`Menu` is antd's [Menu](https://ant.design/components/menu), re-exported unchanged.

- Extends: antd **Menu** — for inherited props and demos, use the antd MCP (`antd_info Menu`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
