---
title: Pagination
description: antd Pagination, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Pagination } from '@nanisoft/prism-ui/components';
```

`Pagination` is antd's [Pagination](https://ant.design/components/pagination), re-exported unchanged.

- Extends: antd **Pagination** — for inherited props and demos, use the antd MCP (`antd_info Pagination`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
