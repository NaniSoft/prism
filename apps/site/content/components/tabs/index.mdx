---
title: "Tabs"
description: "Keyboard-navigated panels for peer views of the same context."
---
{/* prism:generated-owned v2 */}

```ts
import { Tabs } from '@nanisoft/prism-ui/components/tabs';
```

Tabs is a Prism-owned component. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.
