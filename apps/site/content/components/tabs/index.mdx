---
title: Tabs
description: antd Tabs, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Tabs } from '@nanisoft/prism-ui/components';
```

`Tabs` is antd's [Tabs](https://ant.design/components/tabs), re-exported unchanged.

- Extends: antd **Tabs** — for inherited props and demos, use the antd MCP (`antd_info Tabs`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
