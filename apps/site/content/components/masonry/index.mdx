---
title: Masonry
description: antd Masonry, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Masonry } from '@nanisoft/prism-ui/components';
```

`Masonry` is antd's [Masonry](https://ant.design/components/masonry), re-exported unchanged.

- Extends: antd **Masonry** — for inherited props and demos, use the antd MCP (`antd_info Masonry`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
