---
title: DatePicker
description: antd DatePicker, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { DatePicker } from '@nanisoft/prism-ui/components';
```

`DatePicker` is antd's [DatePicker](https://ant.design/components/date-picker), re-exported unchanged.

- Extends: antd **DatePicker** — for inherited props and demos, use the antd MCP (`antd_info DatePicker`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
