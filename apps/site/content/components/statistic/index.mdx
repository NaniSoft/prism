---
title: Statistic
description: antd Statistic, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Statistic } from '@nanisoft/prism-ui/components';
```

`Statistic` is antd's [Statistic](https://ant.design/components/statistic), re-exported unchanged.

- Extends: antd **Statistic** — for inherited props and demos, use the antd MCP (`antd_info Statistic`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
