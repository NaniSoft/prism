---
title: Switch
description: antd Switch, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Switch } from '@nanisoft/prism-ui/components';
```

`Switch` is antd's [Switch](https://ant.design/components/switch), re-exported unchanged.

- Extends: antd **Switch** — for inherited props and demos, use the antd MCP (`antd_info Switch`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
