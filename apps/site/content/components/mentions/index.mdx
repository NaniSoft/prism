---
title: Mentions
description: antd Mentions, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Mentions } from '@nanisoft/prism-ui/components';
```

`Mentions` is antd's [Mentions](https://ant.design/components/mentions), re-exported unchanged.

- Extends: antd **Mentions** — for inherited props and demos, use the antd MCP (`antd_info Mentions`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
