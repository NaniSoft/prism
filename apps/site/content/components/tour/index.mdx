---
title: Tour
description: antd Tour, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Tour } from '@nanisoft/prism-ui/components';
```

`Tour` is antd's [Tour](https://ant.design/components/tour), re-exported unchanged.

- Extends: antd **Tour** — for inherited props and demos, use the antd MCP (`antd_info Tour`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
