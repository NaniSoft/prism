---
title: Empty
description: antd Empty, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Empty } from '@nanisoft/prism-ui/components';
```

`Empty` is antd's [Empty](https://ant.design/components/empty), re-exported unchanged.

- Extends: antd **Empty** — for inherited props and demos, use the antd MCP (`antd_info Empty`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
