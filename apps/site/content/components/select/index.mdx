---
title: "Select"
description: "A type-safe popup select for predefined values."
---
{/* prism:generated-owned v2 */}

```ts
import { Select } from '@nanisoft/prism-ui/components/select';
```

Select is a Prism-owned component. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.

<ComponentDemo id="basic" title="Select" />
