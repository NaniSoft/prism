---
title: Select
description: antd Select, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Select } from '@nanisoft/prism-ui/components';
```

`Select` is antd's [Select](https://ant.design/components/select), re-exported unchanged.

- Extends: antd **Select** — for inherited props and demos, use the antd MCP (`antd_info Select`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
