---
title: Badge
description: antd Badge, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Badge } from '@nanisoft/prism-ui/components';
```

`Badge` is antd's [Badge](https://ant.design/components/badge), re-exported unchanged.

- Extends: antd **Badge** — for inherited props and demos, use the antd MCP (`antd_info Badge`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
