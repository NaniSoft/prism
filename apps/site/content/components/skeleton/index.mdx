---
title: Skeleton
description: antd Skeleton, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Skeleton } from '@nanisoft/prism-ui/components';
```

`Skeleton` is antd's [Skeleton](https://ant.design/components/skeleton), re-exported unchanged.

- Extends: antd **Skeleton** — for inherited props and demos, use the antd MCP (`antd_info Skeleton`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
