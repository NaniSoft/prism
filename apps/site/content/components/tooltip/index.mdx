---
title: Tooltip
description: antd Tooltip, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Tooltip } from '@nanisoft/prism-ui/components';
```

`Tooltip` is antd's [Tooltip](https://ant.design/components/tooltip), re-exported unchanged.

- Extends: antd **Tooltip** — for inherited props and demos, use the antd MCP (`antd_info Tooltip`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
