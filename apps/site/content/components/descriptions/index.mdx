---
title: Descriptions
description: antd Descriptions, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Descriptions } from '@nanisoft/prism-ui/components';
```

`Descriptions` is antd's [Descriptions](https://ant.design/components/descriptions), re-exported unchanged.

- Extends: antd **Descriptions** — for inherited props and demos, use the antd MCP (`antd_info Descriptions`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
