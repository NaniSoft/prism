---
title: Alert
description: antd Alert, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Alert } from '@nanisoft/prism-ui/components';
```

`Alert` is antd's [Alert](https://ant.design/components/alert), re-exported unchanged.

- Extends: antd **Alert** — for inherited props and demos, use the antd MCP (`antd_info Alert`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
