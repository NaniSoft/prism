---
title: Grid
description: antd Grid, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Grid } from '@nanisoft/prism-ui/components';
```

`Grid` is antd's [Grid](https://ant.design/components/grid), re-exported unchanged.

- Extends: antd **Grid** — for inherited props and demos, use the antd MCP (`antd_info Grid`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
