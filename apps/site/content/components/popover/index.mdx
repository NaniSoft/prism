---
title: Popover
description: antd Popover, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Popover } from '@nanisoft/prism-ui/components';
```

`Popover` is antd's [Popover](https://ant.design/components/popover), re-exported unchanged.

- Extends: antd **Popover** — for inherited props and demos, use the antd MCP (`antd_info Popover`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
