---
title: Collapse
description: antd Collapse, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Collapse } from '@nanisoft/prism-ui/components';
```

`Collapse` is antd's [Collapse](https://ant.design/components/collapse), re-exported unchanged.

- Extends: antd **Collapse** — for inherited props and demos, use the antd MCP (`antd_info Collapse`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
