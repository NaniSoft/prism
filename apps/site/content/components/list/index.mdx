---
title: List
description: antd List, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { List } from '@nanisoft/prism-ui/components';
```

`List` is antd's [List](https://ant.design/components/list), re-exported unchanged.

- Extends: antd **List** — for inherited props and demos, use the antd MCP (`antd_info List`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
