---
title: Splitter
description: antd Splitter, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Splitter } from '@nanisoft/prism-ui/components';
```

`Splitter` is antd's [Splitter](https://ant.design/components/splitter), re-exported unchanged.

- Extends: antd **Splitter** — for inherited props and demos, use the antd MCP (`antd_info Splitter`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
