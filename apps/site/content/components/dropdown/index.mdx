---
title: Dropdown
description: antd Dropdown, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Dropdown } from '@nanisoft/prism-ui/components';
```

`Dropdown` is antd's [Dropdown](https://ant.design/components/dropdown), re-exported unchanged.

- Extends: antd **Dropdown** — for inherited props and demos, use the antd MCP (`antd_info Dropdown`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
