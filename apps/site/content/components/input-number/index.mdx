---
title: InputNumber
description: antd InputNumber, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { InputNumber } from '@nanisoft/prism-ui/components';
```

`InputNumber` is antd's [InputNumber](https://ant.design/components/input-number), re-exported unchanged.

- Extends: antd **InputNumber** — for inherited props and demos, use the antd MCP (`antd_info InputNumber`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
