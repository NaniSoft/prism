---
title: Radio
description: antd Radio, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Radio } from '@nanisoft/prism-ui/components';
```

`Radio` is antd's [Radio](https://ant.design/components/radio), re-exported unchanged.

- Extends: antd **Radio** — for inherited props and demos, use the antd MCP (`antd_info Radio`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
