---
title: Space
description: antd Space, unchanged — a pass-through re-export. Import from '@nanisoft/prism-ui'.
---
{/* prism:generated-stub v1 */}

```ts
import { Space } from '@nanisoft/prism-ui/components';
```

`Space` is antd's [Space](https://ant.design/components/space), re-exported unchanged.

- Extends: antd **Space** — for inherited props and demos, use the antd MCP (`antd_info Space`).
- Always import from '@nanisoft/prism-ui', never from `antd` directly.

_No additional props beyond the antd base component._
