---
title: "Icon"
description: "Prism-owned SVG icons drawn on one consistent 24px stroke grid."
---
{/* prism:generated-owned v2 */}

```ts
import { Icon } from '@nanisoft/prism-ui/components/icon';
```

Icon is a Prism-owned component. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.
