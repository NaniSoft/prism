---
title: "DocsShell"
description: "A documentation frame with section navigation, article, and table of contents."
---
{/* prism:generated-owned v2 */}

```ts
import { DocsShell } from '@nanisoft/prism-ui/pages/docs-shell';
```

DocsShell is a Prism-owned page. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.

<ComponentDemo id="basic" title="DocsShell" />
