---
title: "SettingsPage"
description: "A complete settings page with section navigation and form actions."
---
{/* prism:generated-owned v2 */}

```ts
import { SettingsPage } from '@nanisoft/prism-ui/pages/settings-page';
```

SettingsPage is a Prism-owned page. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.

<ComponentDemo id="basic" title="SettingsPage" />
