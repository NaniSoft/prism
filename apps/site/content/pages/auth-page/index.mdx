---
title: "AuthPage"
description: "A complete authentication page composed from AuthForm and page chrome."
---
{/* prism:generated-owned v2 */}

```ts
import { AuthPage } from '@nanisoft/prism-ui/pages/auth-page';
```

AuthPage is a Prism-owned page. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.
