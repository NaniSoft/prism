---
title: "DashboardPage"
description: "An application dashboard frame with navigation, metrics, and recent work."
---
{/* prism:generated-owned v2 */}

```ts
import { DashboardPage } from '@nanisoft/prism-ui/pages/dashboard-page';
```

DashboardPage is a Prism-owned page. Its behavior, public props, and Spectral Refraction recipe ship together from `@nanisoft/prism-ui`.

- Import only from `@nanisoft/prism-ui`.
- Keep the component inside `PrismProvider` when it uses an overlay, popup, or nested theme scope.
- Use `className` for layout composition; visual state stays in the Prism recipe and CSS variables.

<ComponentDemo id="basic" title="DashboardPage" />
